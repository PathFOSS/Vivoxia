<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    style="@style/vivoxia_fragment_scroll">

    <androidx.core.widget.NestedScrollView
        android:id="@+id/ncv"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/vivoxia_linear_layout_scrollable">

            <include android:id="@+id/sl_workouts"
                layout="@layout/slider_layout"/>

            <include android:id="@+id/sl_volume"
                layout="@layout/slider_layout"/>

            <include android:id="@+id/sl_reps"
                layout="@layout/slider_layout"/>

            <include android:id="@+id/sl_sets"
                layout="@layout/slider_layout"/>

            <include android:id="@+id/sl_duration"
                layout="@layout/slider_layout"/>

            <include android:id="@+id/sl_rest"
                layout="@layout/slider_layout"/>

            <include android:id="@+id/sl_work"
                layout="@layout/slider_layout"/>

            <include android:id="@+id/sl_effort"
                layout="@layout/slider_layout"/>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <ImageButton
        android:id="@+id/ib_save"
        android:layout_width="@dimen/button_height"
        android:layout_height="@dimen/button_height"
        android:background="@drawable/icon_save"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="@dimen/spacing"
        android:contentDescription="@string/ac_save"/>

</androidx.constraintlayout.widget.ConstraintLayout>